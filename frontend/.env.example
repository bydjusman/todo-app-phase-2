# Frontend environment variables example
# Copy this to `.env` or set equivalent variables in Vercel project settings.

# Public base URL for the deployed backend (used by client-side code)
NEXT_PUBLIC_API_BASE_URL=https://your-backend.example.com

# Server-side backend URL (used by server components / api routes)
BACKEND_API_URL=https://your-backend.example.com

# Optional legacy / fallback
NEXT_PUBLIC_API_URL=https://your-backend.example.com

# Google API key (if used in project)
NEXT_PUBLIC_GOOGLE_API_KEY=your-google-api-key
